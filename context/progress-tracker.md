# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- In progress

## Current Goal

- Spec persistence + download (feature spec 28) implemented; next is wiring the Specs tab UI to surface persisted specs and call the download route.

## Completed

- Installed and configured shadcn/ui.
- Added generated UI primitives: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea.
- Installed Lucide React for icon usage.
- Added shared class merge utility `cn()` in `lib/utils.ts`.
- Configured dark-theme token mappings in globals CSS so shadcn utilities resolve without light defaults.
- Added `EditorNavbar` app-level chrome component with fixed height, left/center/right sections, and sidebar toggle icon states.
- Added `ProjectSidebar` floating slide-in shell with header, close action, tabs (`My Projects`, `Shared`), empty placeholders, and bottom `New Project` CTA.
- Added `EditorDialogShell` reusable dialog styling pattern using existing tokenized colors with support for title, description, and footer actions.
- Added `EditorWorkspaceShell` app-level composition component that renders navbar + floating sidebar and manages sidebar open/close state.
- Added `CanvasSurface` center scaffold component for the editor workspace.
- Added `AiSidebarShell` right-side slide-over shell component for the editor layout.
- Updated `EditorWorkspaceShell` composition to render the left project sidebar, center canvas scaffold, and right AI panel shell.
- Implemented auth (feature-spec 03-auth): `ClerkProvider` wraps root layout with `@clerk/ui` dark theme and CSS variable overrides; `proxy.ts` protects all routes except `/sign-in` and `/sign-up`; two-panel sign-in and sign-up pages created; `app/page.tsx` redirects authenticated users to `/editor` and unauthenticated users to `/sign-in`; editor workspace shell moved to `app/editor/page.tsx`; `UserButton` added to editor navbar right section.
- Implemented feature-spec 04-project-dialogs: added `EditorHome` center content (heading, description, `New Project` button with `Plus` icon) replacing the canvas scaffold in the workspace shell; added `CreateProjectDialog` with live slug preview driven by `lib/slug.ts`; added `RenameProjectDialog` with prefilled autofocused input, current name in description, and Enter-to-submit; added `DeleteProjectDialog` as confirmation-only with destructive button; added `useProjectDialogs` hook (`hooks/use-project-dialogs.ts`) managing dialog mode, target, form name, and submitting state; updated `ProjectSidebar` to render owned/shared mock projects (`lib/mock-projects.ts`, `types/project.ts`), reveal rename/delete icon actions on hover for owned items only, and add a mobile-only backdrop scrim that closes the sidebar on tap; wired editor home and sidebar create/rename/delete entry points through the hook in `EditorWorkspaceShell`. Mock data only — no API or persistence.
- Implemented feature-spec 05-prisma: created root Prisma config and schema structure (`prisma.config.ts`, `prisma/schema.prisma`, `prisma/models/project.prisma`) with `ProjectStatus` enum, `Project` model, `ProjectCollaborator` model, relation cascade delete, and required indexes; added cached Prisma singleton at `lib/prisma.ts` that branches on `DATABASE_URL` (`prisma+postgres://` uses Accelerate, otherwise `@prisma/adapter-pg`); installed Prisma runtime dependencies (`@prisma/client`, `@prisma/adapter-pg`, `pg`, `@prisma/extension-accelerate`, `dotenv`); ran migration `init_project_data_layer`, generated client, and verified `npm run build` passes.
- Implemented feature-spec 06-project-apis: added `app/api/projects/route.ts` (`GET` lists current user's projects by `ownerId` ordered by `createdAt` desc; `POST` creates a project with Clerk `userId` as `ownerId`, defaults missing/blank name to `Untitled Project`, leaves IDs to the schema's `cuid()`); added `app/api/projects/[projectId]/route.ts` (`PATCH` validates required `name`, enforces owner-only mutation, otherwise returns `403`; `DELETE` enforces owner-only and returns `204` on success); both return `401` for unauthenticated requests and `404` when the project does not exist; updated `proxy.ts` to short-circuit middleware for `/api/*` paths so route handlers can emit explicit `401`/`403`/`404` codes rather than the Clerk sign-in redirect. Backend only; UI is not wired.
- Implemented feature-spec 07-wire-editor-home: added `lib/projects.ts` server helper (`getProjectsForCurrentUser` queries Prisma for owned projects by Clerk `userId` and shared projects via `ProjectCollaborator.email` matched against the current user's primary email, returning each with an `"owned" | "shared"` flag); converted `app/editor/page.tsx` to an async server component that fetches both lists and passes them to `EditorWorkspaceShell`; added `app/editor/[projectId]/page.tsx` workspace route that loads the project, enforces owner-only access via `notFound()`, fetches the sidebar lists, and passes `activeProjectId` to the shell; extended `POST /api/projects` to accept an optional `id` so the client-generated room ID becomes the project ID (room ID = `${slug}-${6-char base36 suffix}`); replaced `MockProject` with `ProjectSummary` (`{ id, name, ownership }`) in `types/project.ts`; deleted `lib/mock-projects.ts` and `hooks/use-project-dialogs.ts`; added `hooks/use-project-actions.ts` (`useProjectActions`) which owns dialog mode/target/name/isSubmitting state, generates the room ID, and calls `POST /api/projects`, `PATCH /api/projects/[id]`, and `DELETE /api/projects/[id]` — create navigates with `router.push` to `/editor/${project.id}`, rename `router.refresh()`s, delete redirects to `/editor` if the deleted project matches `activeProjectId`, otherwise `router.refresh()`s; updated `ProjectSidebar` to consume `ownedProjects` / `sharedProjects` props of `ProjectSummary[]`, displaying the project ID in the slug slot; rewired `EditorWorkspaceShell` to accept server-fetched lists plus `activeProjectId` and pipe the hook through the existing create/rename/delete dialogs.
- Implemented feature-spec 08-editor-workspace-shell: renamed the dynamic editor route from `app/editor/[projectId]/page.tsx` to `app/editor/[roomId]/page.tsx`; added `lib/project-access.ts` exporting `getCurrentIdentity()` (returns Clerk `{ userId, email }` or `null`) and `getProjectForUser(projectId, identity)` (loads the project with collaborators and returns it only when the identity is owner or collaborator, otherwise `null`); added `components/editor/access-denied.tsx` — centered layout, Lock icon in a rounded surface, short message, and a `Back to projects` link to `/editor`; rewrote `app/editor/[roomId]/page.tsx` to redirect unauthenticated requests to `/sign-in`, render `<AccessDenied />` when the project is missing or the identity lacks access (replacing the prior `notFound()` path that also covered the owner-only case), and forward `projectName` plus `activeProjectId` into `EditorWorkspaceShell`; extended `EditorNavbar` with `projectName`, `isAiSidebarOpen`, and `onAiSidebarToggle` props — the navbar now renders the project name centered in the middle column and surfaces a `Share` button (no behavior yet) and a `Sparkles` AI-toggle button on the right when a project is active; updated `EditorWorkspaceShell` to manage `isAiSidebarOpen` state, render `CanvasSurface` in place of `EditorHome` when `activeProjectId` is set, and only mount the right `AiSidebarShell` slide-over for project routes; extended `ProjectSidebar` with `activeProjectId` and applied an `aria-current="page"` highlight (`bg-bg-subtle` + inset ring) to the matching item in both `My Projects` and `Shared` tabs. No canvas logic, Liveblocks, AI chat, or sharing behavior was added — the spec scope is the shell only.
- Implemented feature-spec 10-liveblocks-setup: installed `@liveblocks/node` (the other Liveblocks packages were already present); filled out `liveblocks.config.ts` with `Presence` (`cursor: { x, y } | null`, `isThinking: boolean`) and `UserMeta` (`id` plus `info: { name, avatar, color }`); added `lib/liveblocks.ts` with a lazy, globally cached `getLiveblocksClient()` (reads `LIVEBLOCKS_SECRET_KEY` at first call so `next build` page-data collection does not require the secret) and `getCursorColorForUser(userId)` which deterministically maps a Clerk user ID to one of 10 fixed palette colors via a stable string hash modulo the palette length; added `POST /api/liveblocks-auth` which calls `currentUser()` (returns `401` if missing), reads the `room` field from the JSON body (returns `400` if absent), calls `getProjectForUser(roomId, { userId, email })` and returns `403` if no access, calls `liveblocks.getOrCreateRoom(roomId, { defaultAccesses: [] })` (private room, owner/collaborator access enforced at the auth endpoint rather than via room ACLs), and returns `liveblocks.identifyUser(userId, { userInfo: { name, avatar, color } })` where `name` is `firstName lastName` → `username` → primary email, `avatar` is `user.imageUrl`, and `color` comes from the deterministic palette mapping. No Liveblocks providers or hooks are wired into the client yet — that lives with the canvas implementation.
- Implemented feature-spec 11-base-canvas: added `types/canvas.ts` with `CANVAS_NODE_TYPE` / `CANVAS_EDGE_TYPE` string constants, `NODE_COLORS` (8 dark-fill + vivid-text pairs from `ui-context.md`), `NODE_SHAPES` (`rectangle`, `diamond`, `circle`, `pill`, `cylinder`, `hexagon`), and the typed `CanvasNodeData` (`label`, `color`, `shape`), `CanvasNode`, and `CanvasEdge` aliases over `@xyflow/react` `Node`/`Edge`; added `components/editor/canvas-room.tsx` (`"use client"`) that wraps the room in `LiveblocksProvider` (`authEndpoint: "/api/liveblocks-auth"`), `RoomProvider` (`id={roomId}`, `initialPresence={{ cursor: null, isThinking: false }}`), `ErrorBoundary` (`react-error-boundary`) with a connection-error fallback, and `ClientSideSuspense` with a "Connecting to canvas…" fallback, all rendered inside a `h-[calc(100vh-3.5rem)] p-4` rounded card matching the prior placeholder bounds; added `components/editor/canvas-flow.tsx` (`"use client"`) that calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: { initial: [] }, edges: { initial: [] } })` and renders `ReactFlow` with `connectionMode={ConnectionMode.Loose}`, `fitView`, a `<Background variant={BackgroundVariant.Dots} gap={20} size={1} />`, and `<MiniMap pannable zoomable />`; imported `@xyflow/react/dist/style.css`, `@liveblocks/react-ui/styles.css`, and `@liveblocks/react-flow/styles.css` from the flow component; deleted `components/editor/canvas-surface.tsx` (placeholder) and wired `EditorWorkspaceShell` to render `<CanvasRoom roomId={activeProjectId} />` whenever a project is active; installed `react-error-boundary`. `npm run build` passes. No controls, custom node/edge rendering, persistence, or AI behavior yet (per spec scope limits).
- Implemented feature-spec 09-share-dialog: added `lib/collaborators.ts` exporting `EnrichedCollaborator` (`{ email, name, imageUrl }`) and `enrichCollaboratorsWithClerk(emails)` which calls Clerk's backend `users.getUserList({ emailAddress })` to attach display name (`firstName lastName`) and `imageUrl`, falling back to `{ email, name: null, imageUrl: null }` on lookup failure or for emails with no matching Clerk user; added `app/api/projects/[projectId]/collaborators/route.ts` with `GET` (owner or collaborator can read; returns `{ collaborators: EnrichedCollaborator[], isOwner }` ordered by `ProjectCollaborator.createdAt` asc), `POST` (owner-only — validates email via `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, lowercases, inserts a `ProjectCollaborator` row, returns `409` on duplicate via Prisma `P2002`), and `DELETE` (owner-only — reads `?email=` query param, removes via composite `projectId_email` key, returns `204`); added `components/editor/share-dialog.tsx` rendering through `EditorDialogShell` with a read-only project link `${origin}/editor/${projectId}` plus a `Copy/Copied!` toggle (1.5s revert), an owner-only invite form (email input + button, inline error for `409`/`400`/network), and a collaborator list with avatar (Clerk `imageUrl` or initials fallback), name + email or email-only when no Clerk match, and a per-row trash icon visible to owners only with optimistic-removal-with-revert on error; extended `EditorNavbar` with `onShareClick`; wired `EditorWorkspaceShell` to own `isShareOpen` state, pass `onShareClick={() => setIsShareOpen(true)}` to the navbar only when a project is active, mount `<ShareDialog>` for active projects, and accept a new `isOwner` prop forwarded to the dialog; updated `app/editor/[roomId]/page.tsx` to compute `isOwner = project.ownerId === identity.userId` and pass it down. No local user table was added — Clerk remains the source of truth for user identity.
- Implemented feature-spec 12-shape-panel: extended `types/canvas.ts` with `DEFAULT_NODE_COLOR` (`"neutral"`), `CanvasNodeSize`, `NODE_SHAPE_DEFAULT_SIZES` (rectangle `160×80`, diamond `140×140`, circle `100×100`, pill `160×60`, cylinder `120×100`, hexagon `120×110`), `CANVAS_DRAG_MIME_TYPE` (`"application/x-canvas-shape"`), and `CanvasShapeDragPayload` (`{ shape, width, height }`); added `components/editor/canvas-node.tsx` exporting `CanvasNode` — a `NodeProps<CanvasNodeType>` renderer that fills its parent (`h-full w-full`), uses the matching `NODE_COLORS` entry for fill/text (falling back to neutral), shows the label centered, and registers source+target `Handle`s on all four sides with `opacity-0` per `ui-context.md` (hidden by default); added `components/editor/shape-panel.tsx` — a `pointer-events-none` absolute overlay at `bottom-6 left-1/2 -translate-x-1/2 z-10` containing a `pointer-events-auto` pill `rounded-full border border-surface-border bg-bg-elevated/90 p-1.5 shadow-lg backdrop-blur` with six draggable buttons backed by Lucide `Square`, `Diamond`, `Circle`, `Pill`, `Cylinder`, `Hexagon` icons that call `event.dataTransfer.setData(CANVAS_DRAG_MIME_TYPE, JSON.stringify(payload))` and set `effectAllowed = "move"`; rewrote `components/editor/canvas-flow.tsx` to wrap everything in `<ReactFlowProvider>` (`CanvasFlow`) and split the binding logic into `CanvasFlowInner` which calls both `useReactFlow<CanvasNode, CanvasEdge>()` and `useLiveblocksFlow`, registers a module-level `nodeTypes = { [CANVAS_NODE_TYPE]: CanvasNode }`, and renders a relative wrapping div with `onDragOver` (`preventDefault` + `dropEffect = "move"`) and `onDrop` handlers around `<ReactFlow>` plus the `<ShapePanel />`; on drop the handler reads the MIME payload, validates `shape ∈ NODE_SHAPES` and numeric width/height, converts the cursor via `screenToFlowPosition({ x: clientX, y: clientY })`, generates `id = ${shape}-${Date.now()}-${counter}` (module-scoped `dropCounter` increments per drop), positions the new node centered on the cursor (`position.x - width/2`, `position.y - height/2`), sets `style: { width, height }`, defaults `data` to `{ label: "", color: DEFAULT_NODE_COLOR, shape }`, and dispatches it via `onNodesChange([{ type: "add", item: newNode }])` so the change flows through Liveblocks Storage. `npm run build` passes. Shape-specific SVG visuals (diamond, hexagon, cylinder per `ui-context.md`) are intentionally deferred — every shape renders as a bordered rectangle for this unit.
- Implemented feature-spec 13-node-shape: added `components/editor/shape-background.tsx` — a stateless renderer that produces the visual background for any `CanvasNodeShape` using the matching `NODE_COLORS` palette (falls back to neutral). Rectangle, pill, and circle use a single `absolute inset-0` div with `rounded-xl` (rectangle) or `rounded-full` (pill, circle) and `backgroundColor: palette.fill` inline; diamond, hexagon, and cylinder use inline `<svg>` with `viewBox="0 0 100 100"`, `preserveAspectRatio="none"`, and `vectorEffect="non-scaling-stroke"` so the shape scales with the parent while the stroke stays a constant pixel width. Diamond is a `<polygon points="50,1 99,50 50,99 1,50">`, hexagon is `<polygon points="25,1 75,1 99,50 75,99 25,99 1,50">`, cylinder is two paths — a filled body (`M 1,12 A 49,11 0 0 1 99,12 L 99,88 A 49,11 0 0 1 1,88 Z`) plus a separate top half-ellipse line (`M 1,12 A 49,11 0 0 0 99,12`) so the cap reads as a ring. Border / stroke is subtle at rest (`border-surface-border` for CSS shapes, `var(--border-default)` stroke at width `1` for SVG) and brighter when selected (`border-copy-muted` + `border-2` for CSS shapes, `var(--text-muted)` stroke at width `2` for SVG). Rewrote `components/editor/canvas-node.tsx` to render `<ShapeBackground shape={data.shape} color={data.color} selected={selected ?? false} />` inside a `relative h-full w-full` wrapper, with the four `Handle`s (target Top/Left, source Bottom/Right) kept at `opacity-0`, and the label centered in a `pointer-events-none relative` overlay that uses `palette.text` for color and `truncate` for overflow — the label sits on top of both the shape background and the React Flow handles. Updated `components/editor/shape-panel.tsx` to render a second offscreen container (`fixed -left-[9999px] -top-[9999px]`, `pointer-events-none`, `aria-hidden`) that holds one hidden ghost per `CanvasNodeShape` sized to `NODE_SHAPE_DEFAULT_SIZES[shape]`, styled `opacity-70`, and filled with `<ShapeBackground shape={shape} color={DEFAULT_NODE_COLOR} />`. A `useRef<Partial<Record<CanvasNodeShape, HTMLDivElement | null>>>` collects each ghost element, and `handleDragStart` looks up the matching ghost and calls `event.dataTransfer.setDragImage(preview, width / 2, height / 2)` immediately after `setData`, so the browser attaches a shape-correct ghost to the cursor for the duration of the drag. Drop behavior in `canvas-flow.tsx` is unchanged. `npm run build` passes.
- Implemented feature-spec 15-node-color-toolbar: added `components/editor/node-color-toolbar.tsx` — a stateless swatch row rendering one button per `NODE_COLORS` entry. Each swatch's `backgroundColor` is the palette `fill`, with the matching `text` value forwarded as the inline CSS variable `--swatch-glow` for use by Tailwind arbitrary-value classes. Inactive swatches use `border border-surface-border`; active swatches drop the border and use `ring-2 ring-[var(--swatch-glow)]` so the paired text color reads as a clear selection ring. Hover composes `hover:shadow-[0_0_6px_var(--swatch-glow)]` — a tight 6px-blur glow with no spread, intentionally not wide/blurry per spec. Clicking a swatch calls `useStoreApi().getState().triggerNodeChanges([{ type: "replace", id, item: { ...node, data: { ...node.data, color } } }])`, reusing the same Liveblocks-bound change channel that the label editor already uses (no new API, no server calls); `useReactFlow().getNode(id)` provides the current node so width/height/position/label/shape are preserved on the replace. Clicking the active color is a no-op. The toolbar wrapper carries React Flow's `nodrag nopan` opt-out classes plus `onMouseDown`/`onClick` `stopPropagation` so swatch interactions never start a node drag or pan the canvas. Wired into `components/editor/canvas-node.tsx` by mounting it inside React Flow's built-in `<NodeToolbar isVisible={selected} position={Position.Top} offset={12}>` — the toolbar is portalled into the React Flow viewport, positioned above the node with a 12px gap that clears the `NodeResizer` corner handles, and only renders when the node is selected. The existing palette-driven node background (via `ShapeBackground`) and text color (via `palette.text` on the label span/textarea) already reflect `data.color`, so selecting a swatch updates the node visually on the next render with no additional code. `npm run build` passes.
- Implemented feature-spec 14-node-editing: extended `components/editor/canvas-node.tsx` with resizing and inline label editing while keeping shape rendering, the shape panel, and the drop handler unchanged per spec scope. Resizing uses React Flow's built-in `<NodeResizer />` from `@xyflow/react` mounted as the first child of the node wrapper with `isVisible={selected}` (handles only appear on the selected node), `minWidth={60}` and `minHeight={40}` to prevent collapse, `color="var(--text-muted)"` for the handle background / line border (so the controls read as subtle on the dark canvas), `handleStyle={{ width: 8, height: 8, borderRadius: 2 }}`, and `lineStyle={{ borderColor: "var(--border-default)" }}` for a quiet edge guideline. NodeResizer dispatches `dimensions` changes through React Flow's `triggerNodeChanges` → the same `onNodesChange` already wired to `useLiveblocksFlow`, so resized width/height is persisted to Liveblocks storage as top-level `node.width` / `node.height` (which take priority over the original `style.width/height` set at drop). Inline label editing is owned by the node component: a single `isEditing: boolean` state, a `textareaRef` focused/`select()`ed via `useEffect` when editing opens. The wrapper div listens for `onDoubleClick` and toggles `isEditing` (no-op if already editing). While editing, an absolutely positioned `<textarea rows={1}>` with `nodrag nopan` classes (React Flow's built-in opt-outs for drag and pan) is overlaid centered inside the same `px-4 py-3` box as the label, sharing `palette.text` color, with `bg-transparent`, `border-0`, `outline-none`, and `resize-none`; the existing label `<span>` becomes `invisible` while editing so the box height stays stable and there is no layout shift. The textarea's `value` is bound directly to `data.label` and `onChange` dispatches a `replace` `NodeChange` through `store.getState().triggerNodeChanges([...])` via `useStoreApi<CanvasNodeType, CanvasEdge>()` — keystrokes write through the Liveblocks-bound `onNodesChange` mutation, so every character is synced live (no local draft buffer). `useReactFlow().getNode(id)` provides the current node so the replace change preserves width/height/position; only `data.label` is updated. `onBlur` closes editing; `onKeyDown` closes on `Escape` (with `preventDefault` + `stopPropagation`) and lets Enter insert a newline. The empty-label state shows `"Label"` placeholder text in the same centered position (label span with `opacity: 0.5`), and the same string is set as the textarea's native `placeholder` with `placeholder:opacity-50`. Backspace/Delete inside the textarea is naturally ignored by React Flow's keyboard handler because `isInputDOMNode` short-circuits on `TEXTAREA` elements. `npm run build` passes.
- Implemented feature-spec 17-canvas-ergonomics: added `components/editor/canvas-controls.tsx` — a presentational pill control bar positioned `pointer-events-none absolute bottom-6 left-6 z-10` (bottom-left corner of the canvas, alongside but not overlapping the centered shape panel), with an inner `pointer-events-auto` `rounded-full border border-surface-border bg-bg-elevated/90 p-1.5 shadow-lg backdrop-blur` container mirroring the shape panel's chrome so the two read as the same toolbar family. The bar contains two groups separated by a thin `h-5 w-px bg-surface-border` divider: a zoom group (`ZoomOut`, `Maximize2` for fit view, `ZoomIn` from Lucide) and a history group (`Undo2`, `Redo2`). Each button is a 9×9 `rounded-full` icon button (same dimensions as the shape panel buttons) that flips from `text-copy-muted` to `text-copy-primary` with `bg-bg-subtle` on hover; disabled buttons use `disabled:cursor-not-allowed disabled:opacity-40` with hover styles suppressed so they read clearly dimmed. Added `hooks/use-keyboard-shortcuts.ts` exporting `useKeyboardShortcuts({ reactFlow, onUndo, onRedo })` — a single `useEffect` registers a `window` `keydown` listener that first short-circuits when the event target is an `<input>`, `<textarea>`, `<select>`, or any `contentEditable` element, then routes `Cmd/Ctrl + Z` to `onUndo`, `Cmd/Ctrl + Shift + Z` and `Cmd/Ctrl + Y` to `onRedo`, and `+` / `=` / `-` (with no Cmd/Ctrl/Alt held, so the browser's own `Cmd +` page zoom still works) to `reactFlow.zoomIn` / `zoomOut` with `duration: 200` for a short animated transition. Wired the new pieces into `components/editor/canvas-flow.tsx`: `CanvasFlowInner` now reads `useReactFlow<CanvasNode, CanvasEdge>()`, picks up `useUndo` / `useRedo` / `useCanUndo` / `useCanRedo` from `@liveblocks/react` (callable here because `CanvasFlowInner` is rendered inside the `RoomProvider` in `canvas-room.tsx`), defines `handleZoomIn` / `handleZoomOut` / `handleFitView` callbacks that call `reactFlow.zoomIn` / `zoomOut` / `fitView` with the same `duration: 200`, calls `useKeyboardShortcuts({ reactFlow, onUndo: undo, onRedo: redo })`, and renders `<CanvasControls />` as a sibling of `<ReactFlow>` and `<ShapePanel />` so it overlays the React Flow viewport without being subject to its DOM. `canRedo` / `canUndo` from Liveblocks drive the disabled state on the history buttons. No other canvas surface or behavior changed (per spec scope limits). `npm run build` passes.
- Implemented feature-spec 16-edge-behavior: extended `types/canvas.ts` with a new `CanvasEdgeData extends Record<string, unknown>` (`{ label?: string }`) and re-typed `CanvasEdge = Edge<CanvasEdgeData, typeof CANVAS_EDGE_TYPE>` so edge labels flow through the typed Liveblocks data channel. Updated `components/editor/canvas-node.tsx` to add a `group` class on the wrapper div and replace the old `target Top/Left` + `source Bottom/Right` handle layout with four `Handle type="source"` instances (one per side, ids `top` / `right` / `bottom` / `left`); since `ConnectionMode.Loose` is set at the flow level, source-to-source connections are allowed, so a single source handle per side is enough for any-to-any wiring without stacking. Handles are styled with `!h-2.5 !w-2.5 !rounded-full !bg-white !border-2 !border-[var(--bg-base)]` (small white dot with a dark canvas-colored border), default to `!opacity-0`, and fade in via `transition-opacity duration-150 group-hover:!opacity-100` — `!important` is required because React Flow's `.react-flow__handle` CSS sets size/background inline. Added `components/editor/canvas-edge.tsx` exporting `CanvasEdge` — an `EdgeProps<CanvasEdgeType>` renderer that calls `getSmoothStepPath({ ...endpoints, borderRadius: 10 })` for right-angle routing with rounded corners, and uses the returned `labelX`/`labelY` (not hand-calculated midpoints) for the label position. The visible path is a `<path stroke=... strokeWidth={1.5} strokeLinecap="round" strokeLinejoin="round" markerEnd={markerEnd} className="react-flow__edge-path">` with stroke `var(--text-faint)` at rest and `var(--text-secondary)` when active (hovered, selected, or editing) — `transition: stroke 120ms ease` cross-fades them. A second `<path stroke="transparent" strokeWidth={20} style={{ pointerEvents: "stroke" }} className="react-flow__edge-interaction">` sits on top for a 20px wide hit area; `pointer-events: stroke` lets it capture events even though the stroke is fully transparent, so hovering and double-clicking work without visibly thickening the edge. Local `isHovered: boolean` state is toggled by `onMouseEnter`/`onMouseLeave` on the interaction path and the label container. Label editing reuses the same Liveblocks-bound change channel that the node label/color editors use, but on the edge side: `useStoreApi<CanvasNode, CanvasEdge>().getState().triggerEdgeChanges([{ type: "replace", id, item: { ...edge, data: { ...edge.data, label: next } } }])`, with `useReactFlow().getEdge(id)` providing the live edge so all other fields are preserved. `@liveblocks/react-flow`'s `applyEdgeChanges` handles the `replace` branch through the same `reconcile()` path as nodes, so every keystroke writes through to Liveblocks Storage with no local draft buffer or new API surface. The label is rendered via `<EdgeLabelRenderer>` portalled into React Flow's viewport with `transform: translate(-50%, -50%) translate(${labelX}px, ${labelY}px)`; when not editing, a saved label shows as a `rounded-full border bg-bg-elevated px-3 py-0.5 text-xs` pill badge, an empty active edge shows a faint `Add label` hint badge (`bg-bg-elevated/70 text-copy-faint`), and an empty inactive edge renders nothing. Editing mode mounts an `<input type="text">` with `size={Math.max(label.length || 4, 4)}` so it grows with the text, focused + selected via `useEffect` on mount, saving on blur, Enter, or Escape (Enter and Escape `preventDefault` + `stopPropagation` and close edit mode). The label container uses React Flow's `nodrag nopan` classes plus `onMouseDown`/`onClick` `stopPropagation` so clicking or typing inside the pill never pans the canvas or drags a connected node. Updated `components/editor/canvas-flow.tsx` to register `edgeTypes = { [CANVAS_EDGE_TYPE]: CanvasEdgeRenderer }` and a module-level `defaultEdgeOptions: DefaultEdgeOptions = { type: CANVAS_EDGE_TYPE, markerEnd: { type: MarkerType.ArrowClosed, color: "var(--text-secondary)", width: 18, height: 18 } }` so every connection drawn via a handle becomes a `CANVAS_EDGE_TYPE` edge with the arrow marker already applied; both are passed to the `<ReactFlow>` element. `npm run build` passes.
- Implemented feature-spec 19-presence-avatars-cursors: extended the shared `Presence` type in `liveblocks.config.ts` from `{ cursor, isThinking }` to `{ cursor, thinking }` per spec and updated `RoomProvider`'s `initialPresence` in `components/editor/canvas-room.tsx` to match. Added `components/editor/presence-cursors.tsx` — a `"use client"` component that reads `useOthers()` from `@liveblocks/react/suspense` plus the React Flow viewport transform via `useStore((state) => state.transform)`, and renders one absolutely-positioned `<CursorMarker>` per remote participant whose `presence.cursor` is non-null. Each marker is positioned in screen space using `screenX = cursor.x * zoom + viewportX` (and analogous for `y`), so cursors stay attached to canvas content during pan and zoom. The marker is a 20×20 white-stroked filled SVG arrow tinted to `other.info.color` plus a small same-color name badge — defaults `#808090` and `"Collaborator"` when info is missing. The wrapping container is `pointer-events-none absolute inset-0 overflow-hidden` so cursor visuals never intercept canvas events and never spill outside the viewport. Mounted as a child of `<ReactFlow>` inside `CanvasFlowInner`, which puts it in the same coordinate space the transform is computed against. Added `components/editor/presence-avatars.tsx` — a `"use client"` overlay positioned `absolute right-4 top-4 z-20` inside the canvas section, rendered as a pill (`rounded-full border-surface-border bg-bg-elevated/90 px-2 py-1.5 shadow-lg backdrop-blur`) containing the collaborator stack, a 1px divider, and the Clerk `<UserButton>` (with `appearance.elements.avatarBox`/`userButtonAvatarBox` overridden to `h-7 w-7` so it matches collaborator avatar size). The collaborator list comes from `useOthers()` filtered to drop any entry whose `id` matches the current Clerk `user.id` (resolved from `useUser()` in `@clerk/nextjs`) — this protects against duplicate-tab self-presence even though `useOthers()` already excludes the same WebSocket connection. The first five collaborators render as 28×28 (`h-7 w-7`) avatars in a `-space-x-2` overlapping stack with a `ring-2 ring-bg-elevated` for readability on the dark canvas; collaborators beyond five collapse into a `+N` overflow chip styled with `bg-bg-subtle text-copy-secondary`. Each avatar uses the participant's `info.avatar` URL when present (rendered via raw `<img>` with the lint disable already used in the share dialog) and falls back to a colored disc with two-letter initials backed by `info.color`. The 1px `h-5 w-px bg-surface-border` divider only renders when there is at least one collaborator; with zero collaborators only the `<UserButton>` shows, with no divider. Collaborator avatars are display-only — there is no `onClick`, button wrapper, or popover, only a `title`/`aria-label` for hover identification. Mounted inside `CanvasRoom` as a sibling of `<CanvasFlow>` within the `ClientSideSuspense` (since `useOthers` from the suspense bundle requires it), and inside the `RoomProvider`/`LiveblocksProvider` so the Liveblocks hooks resolve. Live cursor broadcast lives in `components/editor/canvas-flow.tsx`: `CanvasFlowInner` now also calls `useUpdateMyPresence()` from `@liveblocks/react/suspense`, plus an `onMouseMove` handler bound to the `<ReactFlow>` element that converts the native event's `clientX`/`clientY` through `useReactFlow().screenToFlowPosition` and patches `{ cursor: { x, y } }` in flow coordinates (so all clients agree on cursor positions regardless of their own zoom/pan), and an `onMouseLeave` handler that patches `{ cursor: null }`. `<PresenceCursors />` is rendered inside `<ReactFlow>` alongside `<Background>` and `<MiniMap>` so it can read the viewport transform via React Flow's store. Navbar (`components/editor/editor-navbar.tsx`) now only renders the Clerk `<UserButton>` when no project is active (editor home) — when in a project, the avatar group inside the canvas owns the `UserButton` to avoid a duplicate. Existing navbar actions (project name, templates, Share, AI toggle) are unchanged. `npm run build` passes.
- Implemented feature-spec 20-ai-sidebar-shell: exposed the existing AI accent CSS variables to Tailwind by adding `--color-accent-ai: var(--accent-ai)` and `--color-accent-ai-text: var(--accent-ai-text)` to `@theme inline` in `app/globals.css` so the AI sidebar can use `bg-accent-ai` / `text-accent-ai-text` rather than the cyan `brand` palette used elsewhere in the editor; the brand cyan stays for canvas/editor chrome, the indigo `accent-ai` differentiates the AI Workspace. Rewrote `components/editor/ai-sidebar-shell.tsx` (the existing placeholder shell — the floating slide-in chrome, `isOpen`/`onClose` contract, fixed positioning, and `bg-bg-elevated/90 + backdrop-blur` styling are preserved per spec scope). New header is a `flex items-start justify-between` row with a 9×9 rounded-xl icon tile (`bg-accent-ai/20 text-accent-ai-text`) holding a Lucide `Bot` icon, the `AI Workspace` title in `text-copy-primary`, the `Collaborate with Ghost AI` subtitle in `text-copy-muted`, and the existing `X` close button. Underneath is a shadcn `<Tabs defaultValue="architect">` with `TabsList` using `bg-bg-subtle text-copy-muted` and the active tab styled via `data-[state=active]:bg-accent-ai data-[state=active]:text-accent-ai-text`. The `AI Architect` tab is a `flex min-h-0 flex-1 flex-col` with a scrollable message area (`overflow-y-auto`) above the input. When `messages.length === 0`, an empty state renders a 12×12 rounded-2xl `Bot` tile, a short description, and three full-width starter chips (`Design an e-commerce backend`, `Create a chat app architecture`, `Build a CI/CD pipeline`) styled as soft pills using `rounded-full bg-bg-subtle text-accent-ai-text`. Clicking a starter chip calls `sendMessage(prompt)`, which appends a user message to local state — there is intentionally no assistant response (scope limits forbid backend or AI generation). Sent user messages render right-aligned with `border-2 border-brand/50 bg-accent-dim text-copy-primary` and assistant messages render left-aligned with `border border-surface-border bg-bg-elevated text-accent-ai-text` (both bubble styles live in the component so future spec work can drop in assistant turns without restyling). The input area is a single rounded container holding a raw `<textarea rows={1}>` with `min-h-[72px] max-h-[160px]` and a `Send` icon button (`bg-accent-ai text-white`); a `useEffect` keyed on `input` resets the textarea height to `auto` and then sets `style.height = clamp(scrollHeight, 72, 160)` so the field grows with the content up to the cap, after which the textarea scrolls internally. `Enter` calls `sendMessage`, `Shift+Enter` inserts a newline (handled by short-circuiting the keydown only when `!event.shiftKey`); the send button is `disabled` when `input.trim()` is empty. The `Specs` tab renders a `Generate Spec` button (`bg-accent-ai text-white`, leading `Sparkles` icon, no handler yet per scope) and a single static demo card built with `rounded-xl border border-surface-border bg-bg-elevated`: 9×9 rounded `FileText` icon tile, title, two-line description (`line-clamp-2`), and a disabled `Download` button. Used shadcn `Button`, `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` directly; no new shadcn components were added. No wiring to Liveblocks, no backend calls, no AI provider hookup — `EditorWorkspaceShell` already mounts `<AiSidebarShell isOpen onClose>` for active projects, so no upstream wiring changes were needed. `npm run build` passes.
- Implemented feature-spec 21-canvas-autosave: installed `@vercel/blob`. The existing `Project.canvasJsonPath` column already covers the metadata side, so no schema change was needed — the field now holds the Vercel Blob URL returned by `put`. Added `app/api/projects/[projectId]/canvas/route.ts` exposing `PUT` and `GET` against the same path. Both routes reuse `getCurrentIdentity()` + `getProjectForUser()` from `lib/project-access.ts` so owner and collaborator access stays consistent with the rest of the project API surface; unauthenticated → `401`, no access → `404`. `PUT` validates that the body has `nodes` and `edges` arrays (`400` otherwise), uploads `JSON.stringify({ nodes, edges })` to `canvas/${projectId}.json` via `put` with `access: "public"`, `contentType: "application/json"`, `allowOverwrite: true`, and `addRandomSuffix: false` so each save overwrites the same blob (the URL stays stable across saves, and we don't accumulate old blobs), then persists `blob.url` to the project's `canvasJsonPath`. `GET` returns `{ canvas: null }` when the project has no stored canvas yet, otherwise fetches the blob with `cache: "no-store"` and returns `{ canvas }`. Added `hooks/use-canvas-autosave.ts` exporting `useCanvasAutosave({ projectId, nodes, edges, enabled })` and `CanvasSaveStatus` (`"idle" | "saving" | "saved" | "error"`). The hook serializes `{ nodes, edges }` on every change, skips when the serialized payload equals the last successfully saved payload, debounces saves at 1000ms via a `setTimeout` ref that is cleared on each new change, and serializes concurrent saves through an `inFlight` ref — a save started while another is in flight is stashed in a `pending` ref and runs after the current finishes, so the most recent state always lands. `enabled` defaults to true but the canvas flow holds it false until the initial load attempt finishes, so the hook doesn't fire a redundant save right after restoring saved state. Wired both into `components/editor/canvas-flow.tsx`: `CanvasFlow` now requires a `projectId` prop and accepts an optional `onSaveStatusChange` callback; `CanvasFlowInner` runs a `useEffect` keyed only on `projectId` that fires once (`loadAttemptedRef`) — if the room already has any node or edge it skips load (consistent collaboration is in progress) and just flips `autosaveEnabled` to `true`; otherwise it fetches `/api/projects/${projectId}/canvas`, and if the response contains a saved canvas AND the room is still empty it dispatches `add` changes through `onNodesChange` / `onEdgesChange` so the load goes through the Liveblocks-bound channel (the restore is persisted to Storage as the first edit), then `requestAnimationFrame(reactFlow.fitView)` brings the restored design into view. After load (whether or not anything was restored) the hook is enabled. `useCanvasAutosave` then watches `nodes` / `edges` and writes to the PUT route. `saveStatus` is forwarded up via the optional callback so the editor chrome can render an indicator. `components/editor/canvas-room.tsx` threads `projectId` (the existing `roomId` prop, which is the project ID per spec 11) and `onSaveStatusChange` through to the flow. `components/editor/editor-navbar.tsx` adds a `saveStatus?: CanvasSaveStatus` prop and a `SaveStatusBadge` that renders a small `Save` / `Loader2` (spinning during save) / `Check` / `AlertCircle` icon with the text `Saved` / `Saving…` / `Saved` / `Save failed`; the badge sits inside the navbar's right group, only when a project is active, and styles itself with existing `text-copy-muted` / `text-copy-secondary` / `text-red-400` tokens. `EditorWorkspaceShell` owns the new `saveStatus` state (initial `"idle"`), forwards `setSaveStatus` to `CanvasRoom`, and passes the status to `EditorNavbar` only when a project is active. `npm run build` passes.
- Implemented feature-spec 23-design-agent-logic: rewrote `trigger/design-agent.ts` end-to-end so the previously stub task now runs a real Gemini-driven design generator. The task imports `createGoogleGenerativeAI` from `@ai-sdk/google`, `generateObject` + `jsonSchema` from `ai`, `mutateFlow` from `@liveblocks/react-flow/node`, `MarkerType` from `@xyflow/react`, plus `getLiveblocksClient()` from `lib/liveblocks.ts` and the canvas constants (`NODE_COLORS`, `NODE_SHAPES`, `NODE_SHAPE_DEFAULT_SIZES`, `DEFAULT_NODE_COLOR`, `CANVAS_NODE_TYPE`, `CANVAS_EDGE_TYPE`) from `types/canvas.ts`. A union `DesignAction` describes the seven allowed mutations (`add_node`, `move_node`, `resize_node`, `update_node_data`, `delete_node`, `add_edge`, `delete_edge`) and the model is constrained to that surface via a hand-written `jsonSchema<DesignPlan>` — kept JSON Schema rather than zod because zod is not a project dependency and `jsonSchema()` is the documented zero-dep path in AI SDK v6. The system prompt encodes the shape vocabulary (rectangle/diamond/circle/pill/cylinder/hexagon), the palette IDs, layout rules (~240×160 grid centered around origin, ±600 typical span, no overlap, default sizes from `NODE_SHAPE_DEFAULT_SIZES`), edge id convention (`e-<source>-<target>`), and a 20-action cap to keep generations bounded. The plan call passes the current canvas as JSON context so the agent can extend an existing design (read via a no-op `mutateFlow` callback that captures `flow.nodes` / `flow.edges`); `gemini-2.0-flash` is the model id, with the API key read from `GOOGLE_AI_API_KEY` per spec wording. Defensive helpers (`clampPosition` to ±2000, `clampDimension` to 40–600, `safeShape`, `safeColor`) sanitize Gemini output before it lands in storage — so even a partially valid plan stays inside the palette/shape allowlist. Canvas mutations all go through the same `mutateFlow(...)` callback shape that `@liveblocks/react-flow/node` provides — `flow.addNode`, `flow.updateNode`, `flow.updateNodeData`, `flow.removeNode`, `flow.addEdge`, `flow.removeEdge` — which is the exact API spec 22 set up the backend to use; nothing bypasses the existing collaborative flow. Add-node payloads explicitly write `markerEnd: { type: MarkerType.ArrowClosed, color: "var(--text-secondary)", width: 18, height: 18 }` on every new edge so it matches `canvas-flow.tsx`'s `defaultEdgeOptions` (which only applies on `onConnect`, not programmatic adds — same arrow-marker constraint we discovered for starter templates). Node positions are offset by `-width/2, -height/2` so the AI's logical `(x, y)` is the _center_ of the resulting node — keeps the prompt's grid math intuitive without leaking React Flow's top-left-origin convention into the model. Status feed is double-rail: (1) `metadata.set/append` on `status`, `phase`, `nodesAdded`, `edgesAdded`, `currentAction`, `currentActionType`, `summary`, and a running `logs` array — this is what `useRealtimeRun` on the frontend subscribes to; (2) Liveblocks `broadcastEvent` for `ai:start`, `ai:status`, `ai:cursor`, `ai:thinking`, `ai:complete`, and `ai:error` — these flow to every connected room participant, not just the trigger-er. Both rails are clamped behind `safeBroadcast()` so a transient Liveblocks failure (e.g., room not yet warm) only logs a warning instead of aborting the whole generation. Extended `liveblocks.config.ts` `RoomEvent` from `{}` to the discriminated union above so `useEventListener` can type-narrow on `event.kind`. Added `components/editor/ai-presence.tsx` — a thin client component mounted as a sibling of `<MiniMap />` inside `<ReactFlow>` in `canvas-flow.tsx`. It subscribes via `useEventListener`, holds local `{ thinking, cursor }` state, projects the AI's flow-space cursor into screen space using `useStore((s) => s.transform)` (same pattern as `presence-cursors.tsx`), and renders a small `Bot` glyph + `Ghost AI` label tile colored with `bg-accent-ai text-white` so it reads as a _distinct_ agent rather than a human collaborator. A 30s safety timeout resets state if `ai:complete`/`ai:error` are missed. The `ai:start` and `ai:cursor` events do drive the agent's apparent presence; on `add_node` we broadcast the cursor to the new node's center _before_ `flow.addNode`, and on `move_node` we broadcast the target coordinate, so observers see the AI "fly" to each location as it works. Thinking + cursor are both cleared on completion or error. The AI chat sidebar (`components/editor/ai-sidebar-shell.tsx`) — which had been local-state-only since spec 20 — now actually drives the task. `sendMessage` posts to `/api/ai/design` with `{ prompt, roomId: projectId, projectId }` (the existing route from spec 22), exchanges the returned `runId` for a scoped public token via `/api/ai/design/token`, and feeds both into `useRealtimeRun<typeof designAgentTask>(runId, { accessToken, enabled: true })` from `@trigger.dev/react-hooks`. `useRealtimeRun` returns a typed `run` whose `metadata` we read for the live status string (`STATUS_LABELS` maps the task's `status` value → user-facing copy), and the latest assistant message is mutated in place rather than appended so the chat shows a single evolving bubble rather than a stream of duplicates. On `COMPLETED` the bubble switches to `${summary}\n\nAdded N nodes and M edges`; on `FAILED`/`CRASHED` it shows a generic apology. Input + starter chips are disabled while a run is in flight (`disableInput = isSubmitting || activeRun !== null || !projectId`) and the send button swaps to a spinning `Loader2`, so the user can't double-fire. `EditorWorkspaceShell` now threads `activeProjectId` into the sidebar so `triggerDesign` has a project to attach to; when no project is open, the sidebar shows a system-style message ("Open a project to chat with Ghost AI") without ever hitting the API. Scope limits respected: no canvas architecture changes, no new state system outside Liveblocks (events) and Trigger.dev metadata (status feed) — both are existing collaborative-flow primitives that spec 22 already established. `npm run build` passes.
- Improved autosave failure handling in `hooks/use-canvas-autosave.ts`: expected non-OK responses from `PUT /api/projects/[projectId]/canvas` are now handled without throwing a client-side `Error`, the hook records fatal auth/access statuses (`401` / `403` / `404`) to avoid repeated doomed retries in the same session, and console logging now reports structured status/message data instead of surfacing an `Error` object through the Next.js dev overlay.
- Fixed 500 on canvas autosave: the project's Vercel Blob store is configured as private, but `app/api/projects/[projectId]/canvas/route.ts` was calling `put(..., { access: "public" })`, throwing `"Cannot use public access on a private store"` on every save. Switched the PUT call to `access: "private"`, and switched the GET path from an unauthenticated `fetch(canvasJsonPath)` to `get(url, { access: "private" })` from `@vercel/blob` so reads also work against a private store (a direct `fetch` of a private blob URL would 401). Tightened error logging on both branches to serialize `error.message` instead of the raw `Error` object so future blob failures surface clearly. Verified end-to-end against the live store: put → get → delete all succeed with `access: "private"`.
- Implemented feature-spec 18-starter-templates: added `components/editor/starter-templates.ts` exporting the `CanvasTemplate` type (`{ id, name, description, nodes: CanvasNode[], edges: CanvasEdge[] }`) and the `CANVAS_TEMPLATES` array with three diagrams — `microservices` (Client → API Gateway → Auth/User/Order/Payment services → User DB / Order DB / Stripe), `cicd-pipeline` (Source → Build → Test → Deploy Staging → Production, with a Code Review branch and Artifacts cylinder), and `event-driven` (Producer → Event Bus → Order/Email/Analytics workers → Orders DB / SMTP / Warehouse). Each template is built through internal `node()` and `edge()` helpers that source default sizes from `NODE_SHAPE_DEFAULT_SIZES`, set `type: CANVAS_NODE_TYPE` / `CANVAS_EDGE_TYPE`, and attach an explicit `MarkerType.ArrowClosed` `markerEnd` per edge (React Flow `defaultEdgeOptions` only applies to `onConnect`-created edges, not programmatic ones, so the arrow must be set inline for template edges to render with arrowheads). Colors come from the existing `NODE_COLORS` palette. Added `components/editor/starter-templates-modal.tsx` rendering through `EditorDialogShell` with a scrollable `sm:grid-cols-2` grid of cards; each card shows a fixed 280×140 lightweight SVG preview, the template name and description, and an `Import` button that calls `onImport(template)` then `onClose()`. The preview is computed in-component (no React Flow instance): node bounds (min/max x/y including width/height) are projected into the viewport with `Math.min(availableW/contentW, availableH/contentH)` scaling and centered via offsets; edges render as straight `<line>` segments between node centers, and shapes render via a small `PreviewShape` switch (`rect` with `rx` for rectangle/pill/circle, `<polygon>` for diamond/hexagon, three `<rect>` + `<ellipse>` for cylinder). Wired the modal end-to-end through the existing shell architecture: `EditorWorkspaceShell` owns `isTemplatesOpen` state and forwards `onTemplatesClick` to the navbar plus `isTemplatesOpen` / `onTemplatesClose` to `CanvasRoom`; `CanvasRoom` threads both props through `LiveblocksProvider` / `RoomProvider` / `ClientSideSuspense` into `CanvasFlow`, which renders `<StarterTemplatesModal>` as a sibling of `<ReactFlow>` (alongside `ShapePanel` and `CanvasControls`) so the modal lives inside the Liveblocks room context and can dispatch canvas mutations. The `EditorNavbar` gains a `LayoutTemplate` icon button (only visible when a project is active) that triggers `onTemplatesClick`. Import handler in `CanvasFlowInner` clears existing state first (`onNodesChange([{ type: "remove", id }, ...])` followed by the same for edges) and then adds the template's nodes and edges via `add` changes — IDs are namespaced with `Date.now()` (`${originalId}-${stamp}`) on each import so repeated imports never collide, and edge `source`/`target` references are rewritten with the same stamp to stay consistent. All changes go through the Liveblocks-bound `onNodesChange` / `onEdgesChange` channel from `useLiveblocksFlow`, so the replacement syncs to Storage with no new API surface. After applying changes, `requestAnimationFrame` defers a `reactFlow.fitView({ duration: 200 })` call so React Flow sees the new nodes before fitting. `npm run build` passes.

- Implemented feature-spec 28-spec-persistence-download: added a `ProjectSpec` Prisma model (`prisma/models/project.prisma`) with `id @default(cuid())`, `projectId` (FK → `Project` with `onDelete: Cascade`), `filePath` (required `String`), and `createdAt`, plus a `@@index([projectId, createdAt])` for list-by-project queries; added the reverse relation `specs ProjectSpec[]` on `Project`. Applied via `20260521204640_add_project_spec_model` migration and regenerated the client. Metadata only — no spec content in Postgres per spec scope. Extended `trigger/generate-spec.ts` to persist after Gemini returns: a `crypto.randomUUID()` generates the spec ID; the Markdown is uploaded to Vercel Blob at `specs/${projectId}/${specId}.md` via `put` with `access: "private"`, `contentType: "text/markdown"`, `allowOverwrite: false`, `addRandomSuffix: false` (each spec is a new immutable blob — unlike the canvas which overwrites a single stable path); then `prisma.projectSpec.create({ data: { id: specId, projectId, filePath: blob.url } })` records the metadata row using the same id so the blob path and DB id stay aligned. Task output now includes `specId` and `filePath`, and metadata adds a `persisting` phase between `generating` and `complete` so the realtime feed shows the upload step. Blob upload errors and DB write errors both abort the task with structured `logger.error` + a metadata `error` phase. Added `GET /api/projects/[projectId]/specs/[specId]/download` — reuses `getCurrentIdentity()` + `getProjectForUser()` from `lib/project-access.ts` for auth + collaborator access (401 unauthenticated, 404 no access), looks up the spec by `id`, double-checks `spec.projectId === projectId` (returns 404 on mismatch so a valid spec ID from project A cannot be downloaded through project B's URL), fetches the blob via `get(spec.filePath, { access: "private" })` (private store — the canvas route established this same pattern), streams it through `new Response(result.stream).text()`, and returns the Markdown with `Content-Type: text/markdown; charset=utf-8`, `Content-Disposition: attachment; filename="spec-${specId}.md"`, and `Cache-Control: no-store`. No frontend, no spec list endpoint, no Blob URL exposure to clients per spec scope. `npx tsc --noEmit` and `npm run build` pass — `/api/projects/[projectId]/specs/[specId]/download` registered in the build output.

- Implemented feature-spec 27-spec-generation-flow (backend): added `trigger/generate-spec.ts` — a `schemaTask({ id: "generate-spec", schema })` whose Zod payload validates `{ projectId, roomId, chatHistory, nodes, edges }` with caps (chat ≤200, nodes ≤500, edges ≤1000) and accepts the existing `chatMessageSchema` from `types/tasks.ts` so chat history goes through the same shape used by the AI sidebar broadcast feed. Node/edge inner schemas are tolerant (`.passthrough()` on `data`, optional `position`) because the canvas serializes React Flow nodes with `style`, `width`, `height`, `markerEnd`, etc. that the task does not consume — only `id`, `data.label`, `data.shape`, `data.color`, `source`, `target`, `data.label` are read. The task constructs a prompt with three sections (canvas nodes summarised as `- id | "label" | shape=X | color=Y`; canvas edges summarised as `- source -> target (label)`; chat history flattened as `ROLE (sender): content`), calls `generateText({ model: createGoogleGenerativeAI({apiKey})("gemini-2.5-flash"), system, prompt })` from the `ai` package (same provider + model as `trigger/design-agent.ts` but `generateText` rather than `generateObject` because the output is free-form Markdown not a structured plan), trims the result, throws on empty, and returns `{ projectId, roomId, content }` as the task output — Markdown content lives only in the task output for now per spec scope limit "Do not store the final spec in this unit". Metadata updates (`status` / `phase` / `nodeCount` / `edgeCount` / `specLength` / `logs`) fire at every phase so `useRealtimeRun` in a future spec can drive UI without an extra channel. The system prompt fixes top-level section structure (`# Title` → Overview / Goals / Architecture → Components & Data Flow / Interfaces / Data Model / Operational Concerns / Open Questions), forbids inventing components not on the canvas, and tells the model to surface sparseness under "Open Questions" rather than padding. `retry` is 3 attempts with `factor: 1.8` and `randomize: true` to handle transient Gemini errors. Added `POST /api/ai/spec` — accepts `{ roomId, chatHistory, nodes, edges }` (intentionally NOT `projectId` per spec rule "Do not trust a client-supplied projectId"), validates via Zod (passthrough on node/edge so the canvas JSON shape is accepted as-is), resolves project access by calling `getProjectForUser(roomId, identity)` — `roomId` doubles as the project id per the existing spec-07 convention, so the server-derived project id from this lookup is the only value passed to the task. On project miss returns 404 (same shape as the design route); on success fires `tasks.trigger<typeof generateSpecTask>("generate-spec", { projectId: project.id, roomId, chatHistory, nodes, edges })`, persists the resulting `runId` in the existing `TaskRun` model with `userId` from Clerk identity (same row shape used for design runs — no schema change), and returns `{ runId }`. Unlike `/api/ai/design`, this route does NOT mint a token in the same call — the spec calls out a separate `POST /api/ai/spec/token` route for that, so the client must follow up. Added `POST /api/ai/spec/token` — validates `{ runId }`, looks up the `TaskRun` by unique `runId`, enforces `taskRun.userId === identity.userId` (a collaborator who triggered their own run gets a token; another collaborator cannot impersonate them via someone else's `runId`), and mints `triggerAuth.createPublicToken({ scopes: { read: { runs: [taskRun.runId] } }, expirationTime: "1h" })`. Reuses `getCurrentIdentity()` / `getProjectForUser()` from `lib/project-access.ts` rather than the lower-level Clerk `auth()` so collaborator access stays consistent with the rest of the API surface. No frontend, no spec editor UI, no spec persistence, no AI provider abstraction, and no canvas/chat data-model changes per spec scope. `npx tsc --noEmit` and `npm run build` pass — Next.js routes `/api/ai/spec` and `/api/ai/spec/token` are now registered in the build output.

## In Progress

- None currently.

- Design agent backend wiring (spec 22): added `TaskRun` Prisma model (`prisma/models/task-run.prisma`) with `runId @unique`, `projectId`, `userId`, `createdAt`, plus `@@index([runId])` and `@@index([userId, projectId])`; applied via `20260519201033_add_task_run_model` migration and regenerated the client. Added `trigger/design-agent.ts` — minimal `task({ id: "design-agent", run })` accepting `{ prompt, roomId }`, logging via `logger.info`, returning `{ received, prompt, roomId }`. Added `POST /api/ai/design` — validates `{ prompt, roomId, projectId }`, calls `getProjectForUser` to enforce owner/collaborator access, fires `tasks.trigger<typeof designAgentTask>("design-agent", { prompt, roomId })`, persists the resulting run ID in `TaskRun` with `userId` from Clerk identity, returns `{ runId }`. Added `POST /api/ai/design/token` — validates `{ runId }`, loads `TaskRun` by unique `runId`, enforces `userId === identity.userId`, mints a 1h `auth.createPublicToken({ scopes: { read: { runs: [runId] } } })`, returns `{ token }`. No AI logic, no Liveblocks writes, no canvas mutations per spec scope.
- Bootstrapped Trigger.dev: installed `@trigger.dev/sdk` and `@trigger.dev/build`; added `trigger.config.ts` at the repo root pinned to project `proj_kpccbffvkhpqyslxzyph`, `dirs: ["./trigger"]`, `runtime: "node"`, `maxDuration: 300`, and a default retry policy of 3 attempts (1s → 10s, factor 2) with `enabledInDev: false`. The build extension is `prismaExtension({ mode: "modern" })` rather than `mode: "legacy"` because the project uses the Prisma 7 `prisma-client` provider with `@prisma/adapter-pg` — modern mode marks `@prisma/client` as external (zero config, no Rust binaries) which matches what the request handlers already do; legacy mode would have required a schema path and tried to run `prisma generate` itself. Added `trigger/example.ts` with a stub `helloWorld` task using `logger.info` so the dev server has something to register; this will be replaced by the real AI design-generation / spec-generation tasks once their feature specs are scoped. Added `trigger:dev` (`trigger dev`) and `trigger:deploy` (`trigger deploy`) npm scripts. `.mcp.json` already pins `trigger.dev@4.4.6 mcp` for editor tooling. `npx tsc --noEmit` passes.

## Next Up

- Ensure `LIVEBLOCKS_SECRET_KEY` (and public key, if needed) are present in `.env.local` and deployment env so `/api/liveblocks-auth` can mint tokens at runtime.
- Ensure `BLOB_READ_WRITE_TOKEN` is provisioned in `.env.local` and the deployment env so the canvas PUT route can upload to Vercel Blob.
- Set `TRIGGER_SECRET_KEY=tr_dev_...` in `.env.local` (and per-env in production) so `npx trigger dev`, `trigger deploy`, `tasks.trigger()`, and `auth.createPublicToken()` can authenticate against the cloud project `proj_kpccbffvkhpqyslxzyph`.
- Wire the AI sidebar prompt UI to `POST /api/ai/design` → `POST /api/ai/design/token` → `useRealtimeRun` so users can submit design prompts and see run status.
- Add the actual AI generation logic to `trigger/design-agent.ts` (model call, structured node/edge output, Liveblocks Storage writes) — currently a stub that just echoes input.
- Spec persistence is now in place (`ProjectSpec` model, blob upload in `trigger/generate-spec.ts`, `GET /api/projects/[projectId]/specs/[specId]/download`). Next: wire the Specs tab "Generate Spec" button to call `POST /api/ai/spec` → `POST /api/ai/spec/token` → `useRealtimeRun`, render the generated Markdown, list persisted specs per project, and surface the download route on each row.

## Open Questions

- No dedicated feature-spec file currently defines AI panel interaction requirements (input behavior, actions, and visibility controls). Should this be added as `context/feature-specs/06-editor-surface.md` before functional implementation?

## Architecture Decisions

- Add decisions that affect the system design or data model.

## Session Notes

- Editor page now includes full base workspace frame: left floating project sidebar, center canvas scaffold, and right slide-over AI panel shell.
- Auth wired: Clerk dark theme from `@clerk/ui/themes`, CSS variable overrides, `proxy.ts` route guard, `/sign-in` and `/sign-up` two-panel pages, `/` redirect logic, `UserButton` in navbar.
- Editor home is now the center content of `/editor`; the previous `CanvasSurface` scaffold remains in the codebase for later use once a project-open state exists. Project sidebar consumes mock data from `lib/mock-projects.ts`; owned vs shared is the sole gating signal for showing rename/delete actions. Dialog state is centralized in `useProjectDialogs` and lives at `EditorWorkspaceShell`.
- Prisma data layer baseline is now in root runtime paths, not context-only paths: schema/model files are under `prisma/`, client singleton is in `lib/prisma.ts`, and the initial migration has been applied with generated client output in `generated/prisma`.
- Project API routes are backend-only and not wired into the editor UI — the sidebar still consumes mock data from `lib/mock-projects.ts`. Middleware (`proxy.ts`) now skips `auth.protect()` for `/api/*` so route handlers control the response code for unauthenticated/forbidden cases; Clerk continues to protect every non-API route except sign-in/sign-up.
- Editor home (`/editor`) and per-project workspace (`/editor/[projectId]`) are server components that fetch through `lib/projects.ts`; mock data has been removed. Project ID is now intentionally the Liveblocks room ID (`${slug}-${suffix}`), generated client-side in `useProjectActions` and persisted via the optional `id` field on `POST /api/projects`. `Project` schema is unchanged — the slug/room ID lives inside the existing `id` column rather than a separate field. `useProjectActions` is the only dialog hook; create routes the user to `/editor/${projectId}`, rename refreshes, and delete redirects to `/editor` only when the deleted project equals `activeProjectId` (otherwise refreshes). The `[projectId]` workspace page currently checks ownership only and renders the same shell as the home — a real workspace surface (canvas load, collaborator access) is still to come.
- Workspace route is now `/editor/[roomId]` (spec 08 aligned the segment name with the Liveblocks room — the underlying value is still the project ID). Access is centralized in `lib/project-access.ts`: pages call `getCurrentIdentity()` first, redirect to `/sign-in` on null, then call `getProjectForUser(roomId, identity)` and render `<AccessDenied />` (link back to `/editor`) on null. Owner-only restriction is gone — collaborators (matched by primary email against `ProjectCollaborator.email`) can now load the workspace. The shell now branches on `activeProjectId`: with a project, it renders `CanvasSurface` in the center and mounts the right `AiSidebarShell` whose visibility is driven by a new `isAiSidebarOpen` state toggled from the navbar's `Sparkles` button; without one, it keeps the `EditorHome` center and hides the AI panel entirely. The navbar's middle column shows the project name; right-side `Share` and AI-toggle buttons only render when a project is active. Sidebar items receive `activeProjectId` and gain an `aria-current="page"` highlight (subtle ring + elevated background). Sharing behavior and real canvas/AI logic remain unimplemented per spec 08 scope.
- Sharing is now wired end-to-end. Page → shell forwards `isOwner` (`project.ownerId === identity.userId`); the navbar `Share` button opens `<ShareDialog>` which fetches `/api/projects/[projectId]/collaborators` on open. Owners see the invite form and per-row remove buttons (optimistic with revert); collaborators see the list only and no actions. Collaborator rows render Clerk display name + avatar when available and fall back to the raw email — there is intentionally no local users table; `lib/collaborators.ts` resolves names/images through Clerk's backend `users.getUserList({ emailAddress })` at request time, and every lookup or Clerk-client init failure degrades to `{ name: null, imageUrl: null }`. Emails are stored lowercase and validated server-side; the API returns `409` for duplicate invites and `403`/`404` paths match the existing project routes' shape. The `DELETE` endpoint uses `?email=` rather than a path segment to avoid issues with dot characters in dynamic segments. Project URL copy uses `navigator.clipboard.writeText` with a 1.5s `Copied!` toast — silently no-ops in non-secure contexts where clipboard is unavailable.
- Liveblocks infra is set up server-side only — no React provider, no `useMyPresence`/`useOthers` wiring, no canvas binding. `lib/liveblocks.ts` is intentionally lazy because module-level `new Liveblocks({ secret })` broke `next build`'s page-data collection (no env present); `LIVEBLOCKS_SECRET_KEY` is now read on first call. The auth route reuses `getProjectForUser` from spec 08 so owner/collaborator gating stays in one place — `defaultAccesses: []` keeps every room private and the auth endpoint is the sole gate. Cursor color is derived purely from `user.id` (stable string hash → palette index), so the same user shows the same color across reload, browser, and machine without any persisted preference. Spec 10 mentioned "All required Liveblocks packages are already installed" but `@liveblocks/node` was missing and had to be added.
- Canvas wrapper is two files by design: `canvas-room.tsx` owns the providers/Suspense/ErrorBoundary chrome and the layout (rounded card under the navbar), `canvas-flow.tsx` owns the React Flow + `useLiveblocksFlow` binding. Splitting keeps the suspending child below the boundary and lets future canvas chrome (toolbar, presence) attach without touching the flow itself. `useLiveblocksFlow` is invoked with `suspense: true` and empty `nodes`/`edges` initials so multiple clients joining an empty room don't race on a non-empty bootstrap. Imports the three required CSS bundles (`@xyflow/react`, `@liveblocks/react-ui`, `@liveblocks/react-flow`) from `canvas-flow.tsx` only. `Presence` already includes `isThinking: false` from spec 10's `Liveblocks` types, so `initialPresence` sets both `cursor: null` and `isThinking: false` to satisfy the strict typing. `ConnectionMode.Loose` (per spec) lets handles connect freely from any side. Imports for `LiveblocksProvider`, `RoomProvider`, and `ClientSideSuspense` come from `@liveblocks/react/suspense` — using the suspense export is required for the suspending Liveblocks Storage / React Flow hooks downstream. `react-error-boundary` was added as a new dependency since the Liveblocks docs prescribe it for connection-failure fallbacks. The previous `CanvasSurface` placeholder is deleted, not preserved — the new `CanvasRoom` replaces it inline in `EditorWorkspaceShell`. `roomId` is the existing `activeProjectId` (room ID == project ID, per spec 07).
- Auth flow switched from ID tokens to access tokens. Spec 10 originally called `liveblocks.identifyUser(userId, ...)` with `getOrCreateRoom(roomId, { defaultAccesses: [] })`. That combination produced WebSocket close code 4001 ("You have no access to this room") because the room is private and the user is never added to `usersAccesses`. Rather than maintaining `usersAccesses` per collaborator on every auth call, the route now uses `liveblocks.prepareSession(userId, { userInfo }).allow(roomId, session.FULL_ACCESS)` and returns the result of `session.authorize()`. The room ACL is gone — `getProjectForUser` in our route is the single gate, computed per request from Prisma + Clerk. Rooms now auto-create on first connection (no `getOrCreateRoom` call). The `userInfo` shape (`{ name, avatar, color }`) is identical between the two flows, so client-side `useSelf`/`useOthers` consumers are unaffected.
- Shape panel (spec 12): bottom-center pill toolbar lives inside the canvas surface, rendered as a sibling of `<ReactFlow>` (not as a React Flow `Panel`), so its DOM is outside the flow viewport but visually overlays it via absolute positioning. `canvas-flow.tsx` is now wrapped in `<ReactFlowProvider>` and split into `CanvasFlow` (provider) + `CanvasFlowInner` (`useReactFlow` + `useLiveblocksFlow`); this is required because `useReactFlow().screenToFlowPosition` must be called from inside the provider context that also owns the drop wrapper. Drag payload uses a custom MIME type `application/x-canvas-shape` (`CANVAS_DRAG_MIME_TYPE`) to avoid clashing with React Flow's built-in DnD events, and carries `{ shape, width, height }` (`CanvasShapeDragPayload`). Default sizes live in `NODE_SHAPE_DEFAULT_SIZES` in `types/canvas.ts` (rectangles wider than tall, circles square, diamonds slightly larger). Node IDs are `${shape}-${Date.now()}-${counter}` where the counter is a module-scoped integer — the timestamp keeps IDs unique across reloads, the counter keeps multiple drops in the same millisecond unique. New nodes are added via `onNodesChange([{ type: "add", item }])` rather than a `setNodes` setter; this routes the add through the Liveblocks-bound change handler so it persists to Storage. The custom `CanvasNode` renderer is registered in a module-level `nodeTypes` map (stable reference) and currently renders every shape as a bordered rectangle with the label centered — shape-specific SVGs (diamond, hexagon, cylinder per `ui-context.md`) are intentionally deferred. Lucide's `Square` icon is used for the rectangle since Lucide has no `Rectangle` icon. Drop position centers the node on the cursor by subtracting half the width/height. Drop payload is validated (shape ∈ `NODE_SHAPES`, width/height are numbers) even though it originates from our own UI — JSON in `dataTransfer` is treated as untrusted input per code-standards.
- Node shape rendering (spec 13): shape visuals are owned by `shape-background.tsx`, which is intentionally a pure render module — no `"use client"`, no hooks, no internal state. Both the React Flow node renderer (`canvas-node.tsx`) and the drag ghost (`shape-panel.tsx`) consume the same component so a dropped node and its preview look identical. Rectangle / pill / circle use a single `absolute inset-0` div with Tailwind rounding + the palette fill inline; SVG shapes use `viewBox="0 0 100 100"`, `preserveAspectRatio="none"`, and `vectorEffect="non-scaling-stroke"` — the polygon/path scales to the node's `style.width`/`style.height` (set by the drop handler from `NODE_SHAPE_DEFAULT_SIZES`) while the stroke stays a constant pixel width. Polygon points are inset by 1 unit so the stroke is not clipped at the viewBox edges. Cylinder is drawn as two paths so the cap reads as a visible ring: a filled body path closes through both elliptical arcs, and a second unfilled half-ellipse path overlays the top arc; this avoids the ambiguous "filled disc on top" look you get from a single closed path. Selected-state visuals come from React Flow's `selected: boolean` on `NodeProps` — CSS shapes swap `border border-surface-border` → `border-2 border-copy-muted`, SVG shapes swap stroke from `var(--border-default)` width `1` → `var(--text-muted)` width `2`. Width change is acceptable because the background is absolutely positioned, so the 1px delta doesn't shift label layout. The label sits in a `pointer-events-none relative` overlay above both the background and the four `Handle`s, so the handles' `opacity-0` divs stay clickable for edge creation and the label never blocks them. Drag ghost preview is implemented with HTML5 `dataTransfer.setDragImage`, not a custom state-driven follower: `shape-panel.tsx` renders a second container `fixed -left-[9999px] -top-[9999px]` containing one offscreen ghost per shape sized to `NODE_SHAPE_DEFAULT_SIZES[shape]` and styled `opacity-70`; a `useRef<Partial<Record<CanvasNodeShape, HTMLDivElement | null>>>` collects them, and `handleDragStart` passes the matching element to `setDragImage` with a center offset (`width/2`, `height/2`) so the ghost is centered on the cursor. The browser handles attach-during-drag and cleanup-on-drop/cancel automatically, so no manual `dragend` listener or imperative DOM cleanup is needed. Drop behavior in `canvas-flow.tsx` is untouched per spec scope.
- Node color toolbar (spec 15): floats above the selected node via React Flow's built-in `<NodeToolbar>` rather than a manually positioned `absolute bottom-full` element — NodeToolbar portals out of the node's DOM into the viewport, so it's not subject to the node's `overflow` or stacking context, and it handles "only render when selected" via `isVisible={selected}` without an extra `selected && ...` guard around our component. Color updates flow through the same `useStoreApi().getState().triggerNodeChanges([...])` path used by inline label editing, so writes go through `@liveblocks/react-flow`'s `applyNodeChanges` 'replace' branch and land in Liveblocks Storage live with no server call, no local draft state, and no new API surface. Per-swatch glow color is implemented as an inline CSS custom property (`--swatch-glow: palette.text`) on each button, then referenced from Tailwind arbitrary-value classes (`hover:shadow-[0_0_6px_var(--swatch-glow)]`, `ring-[var(--swatch-glow)]`) — this is the only way to have eight different hover/active colors without writing eight Tailwind variants. Active selection uses `ring-2` rather than a thicker border because ring uses an extra box-shadow layer that composes with the hover shadow (both end up in the same `box-shadow` chain), and switching between active/inactive doesn't change box-sizing or trigger layout reflow. `nodrag nopan` opt-out classes plus `mousedown`/`click` `stopPropagation` are both applied — the classes prevent React Flow from initiating a drag/pan, and the stopPropagation guards against any double-click bubbling up to the node wrapper's `onDoubleClick` (which would otherwise put the node into label-edit mode). Selecting the currently active swatch short-circuits before calling `triggerNodeChanges` to avoid an unnecessary Liveblocks write.
- Edge behavior (spec 16): edges are a new custom React Flow edge type (`CANVAS_EDGE_TYPE` = `canvasEdge`) registered through `edgeTypes` on `<ReactFlow>` and applied to every new connection via `defaultEdgeOptions: { type: CANVAS_EDGE_TYPE, markerEnd: { type: MarkerType.ArrowClosed, color: "var(--text-secondary)", width: 18, height: 18 } }`. The arrow marker lives on `defaultEdgeOptions` rather than being hard-coded inside the renderer so a future global edge-style change can swap markers without touching the component. The renderer (`components/editor/canvas-edge.tsx`) does NOT use `<BaseEdge>` — instead it writes two `<path>` elements directly. The visible path uses `getSmoothStepPath({ borderRadius: 10 })` for right-angle routing with rounded corners; the hit-test path duplicates `d` with `stroke="transparent" strokeWidth={20} style={{ pointerEvents: "stroke" }} className="react-flow__edge-interaction"`. `pointer-events: stroke` is the key — a transparent stroke still receives pointer events when this rule is set, which is how React Flow's own BaseEdge interaction overlay works; this gives a wide click/hover target without thickening the visible line. Hover is local component state on the interaction path and label container (not React Flow's `onEdgeMouseEnter`, which would require lifting state to the flow); the active stroke applies for hover OR `selected` OR `isEditing`. Edge label data lives in `data.label` (typed on the `CanvasEdgeData` interface added to `types/canvas.ts`), and writes go through `useStoreApi<CanvasNode, CanvasEdge>().getState().triggerEdgeChanges([{ type: "replace", id, item: { ...edge, data: { ...edge.data, label } } }])` — the same `replace` channel `applyEdgeChanges` in `@liveblocks/react-flow` already routes through `reconcile()`, so every keystroke syncs live with no local draft state or new API. `useReactFlow().getEdge(id)` picks up the current synced edge to preserve source/target/handles. The label is rendered via `<EdgeLabelRenderer>` portalled into the React Flow viewport at the `labelX`/`labelY` coordinates returned by `getSmoothStepPath` (per spec — no manual midpoint math); when not editing it renders a `rounded-full bg-bg-elevated` pill badge, an empty active edge shows a faint `Add label` hint, and an empty inactive edge renders nothing so the canvas stays clean. The editing input uses `size={Math.max(label.length || 4, 4)}` to grow with text, saves on blur / Enter / Escape, and the wrapper carries `nodrag nopan` + `mouseDown`/`click` `stopPropagation` so typing or clicking inside the pill cannot drag a node or pan the canvas. Node handles changed shape: the four-side layout that previously used 2 source + 2 target handles is now 4 source handles, one per side (`top` / `right` / `bottom` / `left`). With `ConnectionMode.Loose` (already set on the flow), source handles can connect to source handles, so a single source per side is sufficient for the spec's "from any handle to any other handle" requirement without stacking two handles at each position. Handle visibility uses `group-hover` on the node wrapper — the wrapper now has `class="group ..."`, and handles use `!opacity-0 transition-opacity duration-150 group-hover:!opacity-100`; `!important` is required because the React Flow `.react-flow__handle` rule sets handle styles via class selectors that otherwise win.
- Issue triage 2026-05-18: (1) share feature wasn't surfacing shared projects because `lib/projects.ts` queried `ProjectCollaborator.email` with the user's Clerk email at its raw case while collaborator rows are always stored lowercase — lowercased the lookup so invitees see the project in the Shared tab; (2) right AI sidebar already had a navbar toggle but no in-panel close affordance — added an `X` close button inside `AiSidebarShell`'s header (mirrors `ProjectSidebar`), wired `onClose` from `EditorWorkspaceShell`, and added an active highlight to the navbar Sparkles toggle so open/close state is visible; (3) canvas dotted pattern was contained inside a `p-4` rounded card — removed the wrapper padding and rounded border from `CanvasRoom` so the React Flow `Background` extends edge-to-edge under the floating sidebars, and added `shadow-2xl shadow-black/40` plus a slightly more transparent `bg-bg-elevated/90` to both sidebars so they read as elevated panels over the canvas.
- Canvas ergonomics (spec 17): the floating control bar lives in `components/editor/canvas-controls.tsx` and is mounted as a sibling of `<ReactFlow>` (alongside `<ShapePanel />`), not as a React Flow `Panel` — that keeps it portal-free and lets `pointer-events-none` on the wrapper + `pointer-events-auto` on the inner pill pass canvas events through the dead space around the toolbar (same pattern as the shape panel). Position is `bottom-6 left-6` — anchored to the bottom-left corner so it never overlaps the centered shape panel at `bottom-6 left-1/2`. The chrome (rounded-full pill, `border-surface-border`, `bg-bg-elevated/90`, `shadow-lg`, `backdrop-blur`) intentionally matches the shape panel's chrome so the two read as part of the same toolbar family. The two button groups are separated by a single 5px-tall `bg-surface-border` divider (`h-5 w-px`); no group label or wrapper — the divider is the only group affordance. Zoom and fit actions all use React Flow instance methods with `{ duration: 200 }` for a short eased transition. Undo / redo / availability state come from Liveblocks via `useUndo`, `useRedo`, `useCanUndo`, `useCanRedo` from `@liveblocks/react` — these hooks track the Liveblocks Storage history that `@liveblocks/react-flow` is already writing into via `useLiveblocksFlow`, so node/edge add/move/resize/label/color edits are all already in the undo stack with no additional batching work; calling these hooks inside `CanvasFlowInner` works because `CanvasRoom` wraps it in `RoomProvider`. Disabled history buttons use `disabled:opacity-40` plus `disabled:hover:bg-transparent disabled:hover:text-copy-muted` to suppress the hover swap, so they read as dimmed/inert; the native `disabled` attribute additionally blocks clicks and Enter activation. Keyboard shortcuts are owned by a dedicated `hooks/use-keyboard-shortcuts.ts` rather than inlined in the controls component, so they fire whenever the canvas is mounted even when the user has not focused the toolbar. The hook ignores events whose target is `INPUT` / `TEXTAREA` / `SELECT` / `contentEditable` — that's the only guard needed because React Flow's own `KeyboardHandlers` use the same `isInputDOMNode` check, so node label editing and edge label editing don't fight the global handler. Zoom shortcuts (`+`, `=`, `-`) intentionally bail out if `Cmd`/`Ctrl`/`Alt` is held so the browser's native page-zoom (`Cmd +` / `Cmd -`) still works; `Shift` is allowed because `+` requires `Shift` on US keyboards. Undo/redo use `Cmd/Ctrl + Z`, `Cmd/Ctrl + Shift + Z`, and `Cmd/Ctrl + Y` — three bindings because Windows users expect `Ctrl + Y` for redo and Mac users expect `Cmd + Shift + Z`. `preventDefault()` is called on every handled binding so the browser's own undo (`Cmd + Z` outside text fields can sometimes trigger "undo close tab" on Firefox) does not fire. React Flow's `<Controls />` was deliberately not used — the spec calls for a custom pill toolbar matching the shape panel chrome, and `<Controls />` ships its own square button styling that doesn't compose with the existing token system.
- AI sidebar shell (spec 20): the floating slide-in chrome was already a separate component (`ai-sidebar-shell.tsx`) controlled by `EditorWorkspaceShell`'s `isAiSidebarOpen` state; this unit only filled out the interior. The whole AI Workspace uses the indigo `--accent-ai` palette rather than the cyan `--accent-primary` brand color, because the AI panel is intentionally a distinct surface from canvas/editor chrome and the project already declared a paired AI accent (`#6457f9` fill + `#8b82ff` text) in `:root`. Those vars were only consumed via inline styles before — exposing them through `@theme inline` (`--color-accent-ai`, `--color-accent-ai-text`) is what lets every part of this sidebar (icon tiles, active tab, starter chips, buttons, assistant message text) reach the AI palette through normal Tailwind utilities. Chat state is intentionally local (`useState<ChatMessage[]>`) and write-only — sending appends a user message, there is no auto-generated assistant reply because spec scope explicitly bars backend, Liveblocks, and AI generation work. Both bubble styles (user: `bg-accent-dim border-brand/50 border-2`, assistant: `bg-bg-elevated border-surface-border text-accent-ai-text`) are still implemented in full so the next spec that wires real assistant turns can render them with no further visual work. Auto-resize uses the classic `style.height = "auto"` → measure `scrollHeight` → clamp(72, 160) pattern inside a `useEffect([input])` rather than a controlled `rows` count, because the latter only supports integer row steps and produces stuttery growth on mixed-length lines; with the clamp pattern the textarea grows pixel-by-pixel up to the cap and then becomes internally scrollable. `Enter` submits and `Shift+Enter` newlines via a single `onKeyDown` that calls `preventDefault()` only when `!event.shiftKey` and `event.key === "Enter"`. Starter chips reuse the same `sendMessage` path so clicking one transitions out of the empty state without any extra plumbing. Spec terminology like `bg-accent`, `bg-subtle`, `bg-elevated`, `bg-brand-dim`, `text-accent-text`, `text-primary-text`, `text-muted-text` was treated as conceptual: the implementation maps them to existing project tokens (`bg-accent-ai`, `bg-bg-subtle`, `bg-bg-elevated`, `bg-accent-dim`, `text-accent-ai-text`, `text-copy-primary`, `text-copy-muted`) rather than inventing new ones — per spec instruction to check `globals.css` / `ui-context.md` before adding direct color values.
- Design agent backend (spec 22): `TaskRun` is a flat tracking table — no FK to `Project` because Trigger.dev's `runId` is the authoritative cross-system identifier and the project relationship is only used for the soft ownership check at trigger time, never for cascading deletes. `userId` is duplicated on `TaskRun` rather than joined through `Project` because the token route needs to authorize _the user who triggered the run_, not the project owner — a collaborator who runs design generation gets to see their own run's stream even if the project owner can also see it through their own trigger calls. Trigger.dev API surface used: `tasks.trigger<typeof designAgentTask>(...)` for fire-and-forget (returns a handle with `.id` — that is the public `runId`), and `auth.createPublicToken({ scopes: { read: { runs: [runId] } } })` for the scoped frontend token. Both routes use `getCurrentIdentity()` + `getProjectForUser()` (the existing `lib/project-access.ts` helpers) instead of the lower-level Clerk `auth()` so collaborator access is enforced consistently with `/api/liveblocks-auth` and the canvas routes. The design task is intentionally a `task()` not a `schemaTask()` because zod is not (yet) a project dependency and the payload already passes through API-route validation — the only caller is our own backend. The route accepts both `roomId` and `projectId` even though they're equal in this codebase (per existing spec 07 convention), because the spec calls them out as separate context fields and a future split (e.g., multi-canvas projects) should not require an API contract change.
- Node editing (spec 14): resize + label edit live entirely in `canvas-node.tsx` — no changes to `canvas-flow.tsx`, `shape-panel.tsx`, `shape-background.tsx`, or types. Resizing piggybacks on React Flow's `<NodeResizer />`, which internally calls `store.getState().triggerNodeChanges([...dimensionChange])`; `triggerNodeChanges` invokes the registered `onNodesChange` (the Liveblocks-bound mutation from `useLiveblocksFlow`), and `@liveblocks/react-flow`'s `applyNodeChanges` switch handles `dimensions` by writing top-level `width` / `height` on the LiveObject node. That means after the first resize, `node.width` / `node.height` are populated and override the original `style.width` / `style.height` set at drop time — React Flow's `getNodeInlineStyleDimensions` reads `node.width ?? node.style?.width`, so both paths render the same size; we intentionally don't bother clearing the now-stale `style.width/height` because the precedence rule makes it inert. The same `triggerNodeChanges` channel is reused for label edits: `handleChange` calls `store.getState().triggerNodeChanges([{ type: "replace", id, item: { ...node, data: { ...node.data, label } } }])`, which routes through Liveblocks' `applyNodeChanges` 'replace' branch (`existing.reconcile(change.item, config)`), syncing every keystroke without any local draft state — the textarea's `value` is bound directly to `data.label`. `useReactFlow().getNode(id)` is used to pick up the current synced node (including width/height/position) so the replace change preserves everything but `data.label`. The textarea uses `nodrag nopan` (React Flow's built-in opt-out classes) to keep typing from initiating a node drag or canvas pan; backspace/delete inside it are also safe because React Flow's NodeWrapper keydown handler short-circuits on `isInputDOMNode` (which lists `TEXTAREA`), so the global delete shortcut won't remove the node mid-edit. Layout shift on edit is avoided by always rendering the label `<span>` and toggling `invisible` while editing — the textarea overlays on top via `absolute inset-0 flex items-center justify-center px-4 py-3`, matching the label container's padding exactly. Empty-label placeholder is rendered as the muted (`opacity: 0.5`) span content `"Label"` when not editing, and as the textarea's native `placeholder="Label"` with `placeholder:opacity-50` when editing. Resize handles are intentionally muted — `color="var(--text-muted)"` for the small 8×8 corner handles with `borderRadius: 2`, and `lineStyle={{ borderColor: "var(--border-default)" }}` for the edge guidelines — so they read as canvas chrome rather than primary UI. `minWidth: 60` / `minHeight: 40` keep nodes legible.
- Implemented feature-spec 25-sidebar-chat-feed: added real-time room chat to the AI sidebar via a new `ai-chat` feed kept fully separate from the existing `ai-status-feed` (`ai:*`) family of room events. Extended `liveblocks.config.ts` `RoomEvent` union with a `chat:message` kind carrying `id: string`, `sender: string`, `role: "user"`, `content: string`, and `timestamp: number` — chat events ride the same Liveblocks broadcast channel as the AI status events but are namespaced via the `chat:*` prefix so consumers can filter on `event.kind` and never mix the two streams. Created `types/tasks.ts` exporting `chatMessageSchema` (Zod v4) — `id` (`min(1)`), `sender` (`min(1).max(120)`), `role` (`z.enum(["user"])`), `content` (`min(1).max(2000)`), `timestamp` (`int().nonnegative()`) — and the inferred TS type `ChatMessage`; this is the single source of truth for the chat message shape, used both when constructing outgoing messages and when validating every incoming event before rendering. Rewrote `components/editor/ai-sidebar-shell.tsx`: removed the design-agent trigger flow (`useRealtimeRun`, `triggerDesign`, `/api/ai/design` + token fetches, `ActiveRun` state, status-label formatting), the AI streaming spinner, and the starter prompt chips per scope limit "don't trigger backend AI tasks" / "don't add AI-generated replies yet". The AI Architect tab is now a collaborative chat: `useBroadcastEvent()` from `@liveblocks/react/suspense` sends `{ kind: "chat:message", ...message }` to the room when the user hits Enter or the send button; `useEventListener` listens for the same kind, runs `chatMessageSchema.safeParse(event)` and silently drops anything that fails validation (incoming events are treated as untrusted input per code standards), then appends to local state via a dedup-by-id setter. Outgoing messages take the same path: a candidate `ChatMessage` is built with `crypto.randomUUID()` for the message id (with a `Date.now() + Math.random()` fallback for non-secure contexts), `sender` resolved from Clerk via a `getSenderName(user)` helper (full name → username → primary email → `null`), `role: "user"`, `content: trimmed`, `timestamp: Date.now()`, validated through the same Zod schema, optimistically appended to local state, and broadcast — `useBroadcastEvent` doesn't echo to the sender, so the optimistic append is what makes own messages appear immediately, and the dedup-by-id guard on the listener defends against any duplicate echo in the future. Input clears on successful send (`setInput("")`) and `sendError` resets; a single `sendError` state surfaces in a `role="alert"` line directly under the input pill with `text-[11px] text-red-400` when the project is missing, the user can't be identified, the candidate fails validation, or `broadcast(...)` throws — kept intentionally small per spec "show a small error state if sending fails". Each rendered message shows sender name, timestamp formatted via `Date.now() → toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })`, and content; bubble styling reuses the existing palette (user/self: `border-2 border-brand/50 bg-accent-dim text-copy-primary`; other: `border border-surface-border bg-bg-elevated text-accent-ai-text`) with own-message detection by comparing the rendered message's `sender` against the current user's resolved `getSenderName` — keeps styling consistent with the existing sidebar UI per spec. To unblock `useBroadcastEvent` / `useEventListener` (which come from the suspense package and need a Suspense boundary in the tree), wrapped the `<AiSidebarShell>` mount in `editor-workspace-shell.tsx` with `<ClientSideSuspense fallback={null}>` — mirrors the wrapping already in place for `<PresenceAvatars />` in the navbar slot, and means the sidebar only mounts once the Liveblocks room session is ready. The textarea auto-resize (`72 → 160px`), Enter-to-submit / Shift+Enter newline behavior, smooth `scrollIntoView` on new messages, the close button, and the unrelated Specs tab placeholder are unchanged from the prior shell. `npm run build` passes.
- Implemented feature-spec 26-ai-chat-functional: wired the AI sidebar (`components/editor/ai-sidebar-shell.tsx`) into the design-agent flow end-to-end. Submitting a prompt now does three things in order — broadcasts the user `chat:message` (unchanged from spec 25), `POST`s `{ prompt, roomId, projectId }` to `/api/ai/design`, and reads `{ runId, publicToken }` from the JSON response into a single `activeRun` state object. While `activeRun` is set and `useRealtimeRun<typeof designAgentTask>(activeRun.runId, { accessToken: activeRun.publicToken, enabled: true })` reports a status in `ACTIVE_RUN_STATUSES` (`WAITING_FOR_DEPLOY`, `DELAYED`, `QUEUED`, `EXECUTING`, `REATTEMPTING`, `FROZEN`, `PAUSED`), the textarea is `disabled`, the send button swaps `Send` for `Loader2` (with `animate-spin`), and a compact status strip renders directly above the input pill — a single-line bar with `border-accent-ai/40 bg-accent-ai/15 text-accent-ai-text` and an animated `bg-brand` ping dot, displaying the latest `ai:status.message` (falling back to `ai:status.status`, and to `ai:start.status` on initial start). The strip is conditionally rendered on `isRunActive` only, so it disappears the moment the run finishes; no persistent UI is left over between prompts. When `run.status` enters `FINAL_RUN_STATUSES` (`COMPLETED`, `FAILED`, `CANCELED`, `CRASHED`, `INTERRUPTED`, `SYSTEM_FAILURE`, `EXPIRED`, `TIMED_OUT`) a final AI `chat:message` is pushed to the `ai-chat` feed via the same broadcast path as user messages — on `COMPLETED` it uses `run.output.summary` (or `"Done."` if empty), on every other terminal status it uses `AI run <status>.` formatted lower-case with underscores → spaces. `activeRun` and `statusMessage` are both cleared, re-enabling the input. To support the AI sender role the `chatMessageSchema` in `types/tasks.ts` was widened from `role: z.enum(["user"])` to `role: z.enum(["user", "ai"])`, and the `chat:message` variant in `liveblocks.config.ts` `RoomEvent` was widened from `role: "user"` to `role: "user" | "ai"`; AI messages use `sender: "Ghost AI"` and render in the existing other-message bubble (`border-surface-border bg-bg-elevated text-accent-ai-text`) — user messages keep the `border-2 border-brand/50 bg-accent-dim` self-bubble, so the distinction is preserved purely through `role` plus the existing own-sender comparison. Status events flow through the existing `useEventListener` — added two new branches (`ai:status` → set `statusMessage` from message or status; `ai:start` → set status to "Starting") alongside the existing `chat:message` branch, and the AI presence indicator (`ai-presence.tsx`) continues to consume its own `ai:start`/`ai:thinking`/`ai:cursor`/`ai:complete`/`ai:error` events untouched. Canvas updates remain entirely Liveblocks-driven via `useLiveblocksFlow` (no manual node/edge sync from the sidebar) per spec scope limit. Errors surface as AI `chat:message` entries in the feed rather than separate UI: a non-2xx `/api/ai/design` response pushes `Could not start the AI run: <error>`; an invalid response shape (missing `runId`/`publicToken`) pushes `Could not start the AI run: invalid server response.`; a fetch throw pushes `Could not start the AI run: <message>`; a realtime `error` from `useRealtimeRun` pushes `Realtime connection error: <message>`. The `sendError` red-text line below the input is now reserved only for client-side pre-flight failures (missing project, no Clerk identity, zod validation failure, broadcast throw) — anything that happens _after_ the user message is committed surfaces as an AI message. To make `POST /api/ai/design` self-sufficient (so the spec's "read `{ runId, publicToken }` from the response" works in one call), extended `app/api/ai/design/route.ts` to call `triggerAuth.createPublicToken({ scopes: { read: { runs: [handle.id] } }, expirationTime: "1h" })` after the task is triggered and the `TaskRun` row is persisted, then return `{ runId, publicToken }`; the separate `/api/ai/design/token` route remains in place for future use cases. Send button color was switched from `bg-accent-ai` to `bg-brand` to match the spec's green-accent submit-button guidance. `npm run build` passes.
- Tightened AI run startup and realtime error handling: `app/api/ai/design/route.ts` now uses the jose-style Trigger.dev token expiry string `"1hr"` when calling `triggerAuth.createPublicToken`, and if token creation fails after `tasks.trigger()` / `prisma.taskRun.create()` the 500 response includes `runId: handle.id` so the started run is still recoverable by the client. `components/editor/ai-sidebar-shell.tsx` now deduplicates `useRealtimeRun()` error handling by caching the last processed `realtimeError.message`, preventing repeated identical connection failures from appending duplicate AI chat messages while still clearing `activeRun` and the status strip on the first occurrence.
